name: build a test project
on:
  push:
    branches:
     - main
     - feature

jobs:
   build:
      runs-on: ubuntu-latest
      steps:
        - name: build a job
          uses: actions/checkout@v4
        - run: |
            echo "Building a job"
